## Date 24/012026
## Day 16 of learning Web development

### 📌 Key Topics Covered

Introduction to list types in HTML (unordered &lt;ul&gt;, ordered &lt;ol&gt;, description lists &lt;dl&gt;).

How default list markers (bullets/numbers) appear.

CSS properties to style lists:

- list-style-type — change bullet/number type (e.g., none, circle, decimal, upper-roman).
- list-style-position — inside vs outside.
- list-style-image — use custom images for bullets.
- Applying margins and padding for spacing and custom list layouts.

Practical examples: navigation menus, styled list cards, custom icons.

### Overflow
What “overflow” means: content exceeding its container.

CSS overflow property values:

- visible — default, content spills out.
- hidden — content is clipped, not shown.
- scroll — always shows scrollbars.
- auto — shows scrollbars only when needed.
- Overflow control on X and Y axes using:
        <ul> <li>overflow-x</li>
        <li> overflow-y</li></ul>
- Real-world uses: image galleries, fixed boxes, scrollable sections.